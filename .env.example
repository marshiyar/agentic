# Secrets Management: https://dotenvx.com
# After adding values, run: dotenvx encrypt
# This encrypts .env (safe to commit) and creates .env.keys (gitignore)
#
# Multi-environment: same keys, different machines
# - macOS/WSL: dotenvx encrypt, share DOTENV_PRIVATE_KEY
# - GCP headless: export in ~/.bashrc or use GCP Secret Manager

# Claude Code (required on headless — macOS sets this during first-run auth)
ANTHROPIC_API_KEY=sk-ant-...

# API Keys (required for multimodel MCP)
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=AI...
VOYAGE_API_KEY=vo-...

# Vertex AI (required for vertex MCP — ADC handles auth, just need project)
GOOGLE_CLOUD_PROJECT=your-project-id
# GOOGLE_CLOUD_LOCATION=us-central1  # optional, defaults to us-central1

# Supabase (optional - for Vault key management and serverless MCP)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...
